package edu.handong.csee.plt;

import edu.handong.csee.plt.ast.AST;
import edu.handong.csee.plt.lfae.value.ExprV;

public class StrictV {
	/*Interpreter interpreter = new Interpreter();

	public AST strict(AST v) {

		if (v instanceof ExprV) {
			System.out.println("expr: " + ((ExprV) v).getExpr().getASTCode());
			return strict(interpreter.interp(((ExprV) v).getExpr(), ((ExprV) v).getDs()));
		}

		return v;
	}*/
}
